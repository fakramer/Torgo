FROM php:8.2-apache

# Install mysqli (and PDO MySQL in case you want it later)
RUN docker-php-ext-install mysqli pdo pdo_mysql \
 && docker-php-ext-enable mysqli

# Optional but handy if you use pretty URLs
RUN a2enmod rewrite
