services:
  web:
    image: php:8.3-apache
    volumes:
      - ./app:/var/www/html
    ports: ["8080:80"]
  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: app
    ports: ["3306:3306"]
